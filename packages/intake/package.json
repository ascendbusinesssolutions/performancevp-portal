{
  "name": "@performancevp/intake",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Turns validated aggregates, ratings and checklists into engine inputs: a pure mirror of the Survey Processing and Scoring Workbook plus the online-only rules",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@performancevp/engine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.13.6",
    "typescript": "6.0.3",
    "vitest": "5.0.1"
  }
}
